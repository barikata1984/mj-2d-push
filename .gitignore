__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
*.pt
*.ckpt
wandb/
runs/
outputs/
logs/
.DS_Store

# Pixi — environment is materialised at runtime; pixi.lock IS tracked.
.pixi/

# Docker — machine-specific files (use .env.example as template)
.devcontainer/.env
.devcontainer/docker-compose.override.yaml

# Claude Code local settings
.claude/settings.local.json

# MuJoCo menagerie (sparse clone, not vendored)
mujoco_menagerie/

# Simulation results — frame images are large; keep data/video/plots only
results/*/pics/

# Symlinks to external assets
assets
